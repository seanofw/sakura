
namespace Sakura.BetterControls.Scrollbar
{
	// Warning: Do not change the numeric values below without updating
	// the logic in BetterScrollbar that depends on them.
	public enum MarkStyle
	{
		None = 0,

		InitialMargin = 1,
		InitialOverlapping = 2,
		FinalOverlapping = 3,
		FinalMargin = 4,
		Cross = 5,
	}
}